Sortings

2.1.Monk and Nice Strings

#include <iostream>
#include <iterator>
#include <set>
using namespace std;

int main()
{
    int n,i,j;
    multiset <string> se;
    multiset <string> :: iterator it;
    cin >> n;
    string s;
    int k;
    for(i=0;i<n;i++)
    {
        cin >>s;
        k=0;
        for(it=se.begin();it!=se.end();++it)
        {
            k+=(*it<s);
        }
        cout << k << endl;
        se.insert(s);
    }
    return 0;
}

public static  void main(strng args[])
{
int n,i,j;
string s;
iterator it;
System.out.println(n);
System.out.println(s);
int count=0;
for(it=s.begin();it!=s.end();++it)
{
System.out.println(s);
{
count+=(*it<s);
}



